export type Product = {
    _id: string
    name: string
    description: string
    image: string
    price: number
}

export type Topping = {
    _id: string
    name: string
    price: number
    image: string
}

